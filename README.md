# 🏆 Betting Integration API

API-сервис для интеграции с платформой ставок.  
Сервис выступает промежуточным слоем между клиентскими приложениями и внешним Betting Provider API.

📌 **Документация API**:  
[![Betting API](https://img.shields.io/badge/API%20Docs-ReadMe-blue)](https://bettingintegrationapi.readme.io/reference/post_auth-login#/)

## 🚀 Особенности

✅ Поддержка JWT-аутентификации  
✅ Работа со ставками (создание, просмотр, рекомендации)  
✅ API для обновления баланса  
✅ Интеграция с внешним провайдером ставок

---

## 📖 Документация API

Полная документация доступна по ссылке:  
🔗 **[API Reference](https://bettingintegrationapi.readme.io/reference/post_auth-login#/)**

### 🔑 Аутентификация

Для работы с API требуется аутентификация через JWT-токен.  
**Запрос**:

```http
POST /auth/login
Content-Type: application/json

{
  "username": "user1"
}
```

⚙ Инструкция по запуску приложения
1. Создай или помести .env-файл в корень проекта.
(Убедись, что все переменные окружения заполнены корректно.)
2. Запусти контейнеры через Docker Compose:
```sh
docker-compose up -d
```

3. Проверь состояние контейнеров:

```sh
docker ps
```

```sh
docker logs betting_api
```

🗂 Коллекция запросов для Postman
В корне проекта находится файл:
akBackend.postman_collection.json
С его помощью можно протестировать работоспособность приложения в Postman.

Команды для запуска тестов:

установка зависимостей
```sh
yarn install
```

запуск jest
```sh
yarn jest
```
очистить кэш тестов
```sh
yarn jest --clearCache
```


Архитектура решения основана на многослойной архитектуре с чётким разделением задач. Используем Express для сервера API, Prisma как ORM для работы с Postgres. Интеграция с внешней платформой для ставок осуществляется через API (externalApiClient), а также предоставляются внутренние эндпоинты для различных функций: аутентификация (на базе JWT), ставки (размещение, обновление и т.д.), баланс, транзакции, проверка состояния, внутренние конечные точки для тестирования интеграции с внешним API.
Система построена на многослойной архитектуре с четким разделением ответственности, что обеспечивает масштабируемость, поддерживаемость и удобство тестирования:
1. Слои и их задачи
	•	API-клиенты (apiClients):
Здесь располагаются модули для взаимодействия с внешними API (например, externalApiClient для ставок, healthApiClient для проверки состояния внешнего сервиса). Эти модули инкапсулируют логику формирования запросов, подписи (с использованием утилиты signature) и обработки повторных попыток при сбоях.
	•	Контроллеры (controllers):
Контроллеры отвечают за обработку HTTP-запросов, вызов бизнес-логики и формирование ответа клиенту. Они не содержат бизнес-логики – только преобразование входных данных и вызов соответствующих сервисов (например, authController, balanceController, betController, healthController, transactionController).
	•	Сервисы (services):
Здесь реализована бизнес-логика приложения. Сервисы (authService, betService, balanceService, transactionService) занимаются проверкой входных данных, обработкой ошибок, вызовами внешнего API через API-клиенты и координацией работы с репозиториями. Например, betService.placeBet не только создает ставку, но и асинхронно обновляет ее результат, а balanceService.getBalance и checkBalance взаимодействуют с внешним API для актуализации баланса.
	•	Репозитории (repositories):
Этот слой отвечает за непосредственное взаимодействие с базой данных через Prisma. Репозитории (userRepository, betRepository, balanceRepository, transactionRepository, apiLogRepository) инкапсулируют CRUD-операции и обеспечивают абстракцию от конкретной СУБД. Например, balanceRepository.upsertUserBalance обновляет или создает запись в таблице user_balances, а apiLogRepository записывает запросы к внешнему API для аудита.
	•	Мидлвары (middlewares):
Мидлвары, например, authenticateToken, обеспечивают безопасность приложения, проверяя JWT-токен в заголовке запроса и устанавливая данные пользователя в объекте запроса. Это позволяет защитить API-эндпоинты от неавторизованных вызовов.
	•	Маршруты (routes):
Маршруты связывают URL с контроллерами, применяя соответствующие мидлвары. Каждый модуль маршрутов (authRoutes, betRoutes, balanceRoutes, checkBalanceRoutes, healthRoutes, internalRoutes, transactionRoutes) отвечает за группу связанных эндпоинтов.
	•	Утилиты (utils):
Утилиты содержат вспомогательные функции, такие как создание HMAC-подписи (signature.ts), форматирование объектов (formatBet.ts, formatTransaction.ts) и другие общие функции, используемые в приложении.
	•	База данных (database):
В папке database находится файл prismaClient.ts, где создается и экспортируется единственный экземпляр PrismaClient для работы с PostgreSQL.
2. Взаимодействие с внешним API и логирование
	•	Интеграция с внешним API:
При выполнении операций, таких как размещение ставки, обновление результата (через эндпоинты /bet, /win, /balance, /check-balance), сервисы используют функцию callExternalAPI из слоя apiClients. Эта функция подготавливает запрос, генерирует подпись с помощью createSignature и отправляет запрос на внешний сервис. Если запрос завершается ошибкой, реализован механизм повторных попыток.
	•	Логирование запросов:
Каждый вызов внешнего API логируется в базу данных через репозиторий apiLogRepository. Таким образом, в таблице ApiLog сохраняется информация о запросе (метод, URL, тело запроса), ответе, статусе, времени выполнения и IP-адресе. Это позволяет отслеживать и анализировать взаимодействие с внешним API.
3. JWT-аутентификация и связывание пользователей
	•	JWT-аутентификация:
При входе через эндпоинт /auth/login генерируется JWT-токен, который содержит идентификатор и имя пользователя. Мидлвар authenticateToken проверяет валидность токена для защищенных эндпоинтов (bets, balance, transactions и др.), обеспечивая безопасность приложения.
	•	Связывание пользователей:
Модель ExternalApiAccount связывает пользователей вашей системы с их учетными записями во внешнем API ставок. При выполнении операций сервисы используют данные из ExternalApiAccount (externalId, secretKey) для формирования запросов к внешнему API, что обеспечивает корректное сопоставление действий пользователя в вашей системе с его аккаунтом на платформе ставок.
4. Тестирование и документация
	•	Тестирование:
Для обеспечения надежности системы используется Jest для unit-тестирования каждого слоя (сервисы, контроллеры, репозитории, утилиты). Наличие интеграционных тестов через Supertest помогает проверить корректность работы API-эндпоинтов. Покрытие кода тестами нацелено на минимум 70–80%.
	•	Документация:
Для удобства разработчиков и пользователей API разработана документация по стандарту OpenAPI (Swagger). Документация описывает все эндпоинты, параметры запросов, форматы ответов и примеры использования.
5. Общее
Архитектура решения ориентирована на гибкость и расширяемость:
	•	Модульность: Каждый компонент (API-клиенты, контроллеры, сервисы, репозитории, утилиты) изолирован и отвечает за свою задачу.
	•	Безопасность: JWT-аутентификация и разделение логики через мидлвары защищают API от несанкционированного доступа.
	•	Интеграция: Механизмы работы с внешним API (включая повторные попытки и логирование) повышают устойчивость системы к временным сбоям.
	•	Тестирование: Хорошее покрытие тестами гарантирует, что при изменениях система будет работать корректно.

Эта архитектура позволяет легко масштабировать и поддерживать проект, а также обеспечивает высокую надежность и безопасность работы сервиса.